# STATUS BAR
# Turn the status bar on
set-option -g status on
# Status bar update interval to 5 seconds
set -g status-interval 5
# Status bar center alignment for windows
set -g status-justify centre

# GENERAL
set -g mouse on
