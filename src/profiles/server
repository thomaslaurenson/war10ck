# shellcheck shell=bash disable=SC2034

NAME="Server"
DESCRIPTION="Generic server environment"

# Modules to apply, one per line.
# A bare name runs both install and config. Append ":install" or ":config"
# to restrict a module to a single step.
MODULES=(
  bash
  git
)
