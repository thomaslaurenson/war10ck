; Polybar configuration

[colors]
background = #0a0a0a
background-alt = #1a1a1a
foreground = #f8f8f8
foreground-dim = #666666
primary = #00FF00
alert = #ff0000
; The phosphor amber of an old console monitor.
amber = #FFB000

[bar/top]
monitor = ${env:MONITOR:}
wm-name = top
wm-stack = false

override-redirect = false

width = 100%
height = 32
radius = 0
fixed-center = true

background = ${colors.background}
foreground = ${colors.foreground}

line-size = 2
line-color = ${colors.primary}

border-size = 0

padding-left = 2
padding-right = 2

module-margin-left = 1
module-margin-right = 1

font-0 = monospace:size=10;2
; The monospace face has no asterisk-burst glyph; DejaVu Sans carries the one
; the Claude modules use.
font-1 = DejaVu Sans:size=10;2

modules-left = workspaces
modules-center =
modules-right = claude-sess claude-week cpu memory pulseaudio wlan eth battery date powermenu

tray-position = none
tray-padding = 4

cursor-click = pointer
cursor-scroll = ns-resize

[module/workspaces]
type = custom/script
exec = ~/.war10ck/polybar/workspaces.sh
tail = true

[module/claude-week]
type = custom/script
exec = ~/.war10ck/polybar/claude-usage.sh week
interval = 300
format-prefix = "✳ WEEK "
format-prefix-foreground = ${colors.primary}

[module/claude-sess]
type = custom/script
exec = ~/.war10ck/polybar/claude-usage.sh session
; Ticks every minute to keep the reset countdown moving. The script caches, so
; this does not mean asking claude every minute.
interval = 60
format-prefix = "✳ SESS "
format-prefix-foreground = ${colors.primary}

[module/cpu]
type = internal/cpu
interval = 2
format-prefix = "CPU "
format-prefix-foreground = ${colors.primary}
label = %percentage:2%%

[module/memory]
type = internal/memory
interval = 2
format-prefix = "MEM "
format-prefix-foreground = ${colors.primary}
label = %percentage_used:2%%

[module/pulseaudio]
type = internal/pulseaudio
interval = 5
format-volume = <label-volume>
format-volume-prefix = "VOL "
format-volume-prefix-foreground = ${colors.primary}
label-volume = %percentage%%
label-muted = VOL muted
label-muted-foreground = ${colors.alert}
click-left = pactl set-sink-mute @DEFAULT_SINK@ toggle
click-right = pavucontrol
ramp-volume-headphones-0 = [HP]

[module/wlan]
type = internal/network
interface = ${env:WIFI_IFACE:}
interval = 3
format-connected-prefix = "WIFI "
format-connected-prefix-foreground = ${colors.primary}
label-connected = %essid%
format-disconnected =

[module/eth]
type = internal/network
interface = ${env:ETH_IFACE:}
interval = 3
format-connected-prefix = "ETH "
format-connected-prefix-foreground = ${colors.primary}
label-connected = %local_ip%
format-disconnected =

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 98

format-charging = <label-charging>
format-charging-prefix = "BAT "
format-charging-prefix-foreground = ${colors.primary}
label-charging = %percentage%%+

format-discharging = <label-discharging>
format-discharging-prefix = "BAT "
format-discharging-prefix-foreground = ${colors.primary}
label-discharging = %percentage%%

format-full = <label-full>
format-full-prefix = "BAT "
format-full-prefix-foreground = ${colors.primary}
label-full = 100%

[module/date]
type = internal/date
interval = 1
date = %a %b %d
time = %H:%M
label = %date%  %time%
label-foreground = ${colors.amber}

[module/powermenu]
type = custom/text
content = PWR
content-foreground = ${colors.alert}
content-padding = 2
click-left = i3-nagbar -t warning -m 'Power?' -B 'Shutdown' 'systemctl poweroff' -B 'Reboot' 'systemctl reboot' -B 'Suspend' 'systemctl suspend' -B 'Lock' 'i3lock'
